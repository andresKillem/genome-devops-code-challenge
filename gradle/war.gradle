apply plugin: "war"

bootWar {
    mainClass = "com.genome.munoz.GenomeApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
